export class UI {
  setReadyState(_hasAudio: boolean) {
    // Deprecated compatibility shim.
  }

  setPlayingState(_isPlaying: boolean) {
    // Deprecated compatibility shim.
  }

  setErrorState(_message: string) {
    // Deprecated compatibility shim.
  }
}
